"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const tslib_1 = require("tslib");
const fs = tslib_1.__importStar(require("fs"));
const windowSize = tslib_1.__importStar(require("window-size"));
const MAX_DISPLAY_DESCRIPTION = windowSize.width - 15;
exports.createTemplate = (name) => `
---
name: '${name}'
description: '${name} of description'
message: 'Please enter any text.'
root: '.'
output: '**/*'
ignore: []
---

# \`{{ input }}.md\`

\`\`\`markdown
Let's create a template with reference to the document!
https://github.com/cats-oss/scaffdog/#templates
\`\`\`
`.trim();
exports.truncate = (value) => `${value.slice(0, MAX_DISPLAY_DESCRIPTION)}${value.length > MAX_DISPLAY_DESCRIPTION ? '...' : ''}`;
exports.fstat = (path) => {
    try {
        return fs.statSync(path);
    }
    catch (e) {
        return null;
    }
};
exports.fileExists = (path) => {
    const stat = exports.fstat(path);
    return stat == null ? false : stat.isFile();
};
exports.directoryExists = (path) => {
    const stat = exports.fstat(path);
    return stat == null ? false : stat.isDirectory();
};
//# sourceMappingURL=utils.js.map